FROM python:latest
WORKDIR /opt/backend
RUN apt-get update \
    && apt-get install -yq --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*


COPY ./src/backend .

RUN pip install -r requirements.txt



EXPOSE 8000
